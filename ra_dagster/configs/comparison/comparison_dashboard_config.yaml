resources:
  database:
    config: "dev"

ops:
  comparison_dashboard_metrics:
    config:
      run_ref_a: "CHANGE_ME"  # Replace with actual run_id or run_ref for first run
      run_ref_b: "CHANGE_ME"  # Replace with actual run_id or run_ref for second run
