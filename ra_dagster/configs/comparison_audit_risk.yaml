ops:
  compare_runs:
    config:
      run_id_a: "RUN_ID_BASELINE_2024"
      run_id_b: "RUN_ID_HIGH_CONFIDENCE"
      run_description: "Audit Risk / Truncation Analysis"
      metric: "mean"
      population_mode: "intersection"

resources:
  duckdb:
    config:
      path: /home/garth/garage/prism/risk_adjustment.duckdb
