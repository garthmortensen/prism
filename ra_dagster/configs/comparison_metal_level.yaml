ops:
  compare_runs:
    config:
      run_id_a: "RUN_ID_BASELINE_2024"
      run_id_b: "RUN_ID_SILVER"
      run_description: "Benefit Design (Metal Level) Impact Analysis"
      metric: "mean"
      population_mode: "intersection"

resources:
  duckdb:
    config:
      path: /home/garth/garage/prism/risk_adjustment.duckdb
