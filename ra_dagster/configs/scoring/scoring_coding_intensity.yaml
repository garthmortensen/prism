ops:
  score_members_aca:
    config:
      # Scenario: Coding Intensity / Chart Review Impact
      # Using an enhanced claims view with simulated added diagnoses
      claims_view: dbt_raw.raw_claims_2024_enhanced
      enrollments_view: dbt_raw.raw_enrollments
      members_view: dbt_raw.raw_members
      invalid_gender: skip
      diy_model_year: "2024"
      member_age_basis_year: "2024"
      run_description: "Coding Intensity Simulation (Enhanced Claims)"

resources:
  database:
    config: "dev"
