ops:
  score_members_aca:
    config:
      # Scenario: Benefit Design Impact (Metal Level)
      # Using a member view where all members are simulated as Silver plan
      claims_view: dbt_raw.raw_claims
      enrollments_view: dbt_raw.raw_enrollments
      members_view: dbt_raw.raw_members
      invalid_gender: skip
      diy_model_year: "2024"
      member_age_basis_year: "2024"
      run_description: "Metal Level Sim (All Silver)"

resources:
  database:
    config: "dev"
