ops:
  score_members_aca:
    config:
      # Scenario: Lag / Runout Analysis
      # Using a claims view filtered to 6 months of runout
      claims_view: dbt_raw.claims_2024_6months
      enrollments_view: dbt_raw.raw_enrollments
      members_view: dbt_raw.raw_members
      invalid_gender: skip
      diy_model_year: "2024"
      member_age_basis_year: "2024"
      run_description: "Lag Analysis (6-month runout)"

resources:
  database:
    config: "dev"
