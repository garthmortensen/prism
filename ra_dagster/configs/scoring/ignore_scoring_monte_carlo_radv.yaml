ops:
  score_members_aca:
    config:
      # Scenario: Monte Carlo RADV Simulation
      # Using "stacked" views that replicate the population N times with random claim dropouts
      claims_view: main_raw.claims_monte_carlo_radv
      enrollments_view: main_raw.enrollments_monte_carlo_radv
      members_view: main_raw.members_monte_carlo_radv
      invalid_gender: skip
      diy_model_year: "2024"
      member_age_basis_year: "2024"
      run_description: "Monte Carlo RADV Simulation (N=50)"

resources:
  duckdb:
    config:
      path: /home/garth/garage/prism/risk_adjustment.duckdb
