ops:
  score_members_aca:
    config:
      # Optional: override where scoring inputs come from.
      # If set, you must set all three.
      claims_view: dbt_raw.raw_claims
      enrollments_view: dbt_raw.raw_enrollments
      members_view: dbt_raw.raw_members
      invalid_gender: skip
      diy_model_year: "2024"
      member_age_basis_year: "2024"
      run_description: "ACA scoring run"

resources:
  database:
    config: "dev"
