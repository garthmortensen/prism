ops:
  score_members_aca:
    config:
      # Scenario: Truncation / Audit Risk
      # Using a claims view filtered to high-confidence providers only
      claims_view: dbt_raw.high_confidence_claims
      enrollments_view: dbt_raw.raw_enrollments
      members_view: dbt_raw.raw_members
      invalid_gender: skip
      diy_model_year: "2024"
      member_age_basis_year: "2024"
      run_description: "Audit Risk (High Confidence Claims Only)"

resources:
  database:
    config: "dev"
